from active_passive_sentence import ActivePassiveSentence

print(ActivePassiveSentence("the application is returned to the applicant/employee who can review the rejection reasons.").convert_passive_to_active_sentence())